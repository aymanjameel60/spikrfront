function currencySheet(){if(!state.currencySheet)return '';const opts=(state.liveCurrencies?.length?state.liveCurrencies.map(c=>(c.code||c.currency_code||'').toUpperCase()).filter(Boolean):['SAR','USD']);return `<div class="shade show" data-action="close-currency"></div><div class="settings-bottom-sheet show"><div class="sheet-handle"></div><div class="settings-sheet-head"><h3>اختر العملة</h3><button class="sheet-close-btn icon-button" data-action="close-currency">${icon('x',20)}</button></div><div class="currency-list">${opts.map(code=>`<button class="currency-option ${state.settings.currency===code?'selected':''}" data-action="select-home-currency" data-value="${code}"><span><strong>${currencyLabel(code,true)}</strong><small>${currencyCodeLabel(code)}</small></span><b class="settings-radio"></b></button>`).join('')}</div></div>`}
function home(){const adminCategories=(Array.isArray(state.spikeContent?.spike_categories)?state.spikeContent.spike_categories:[]).filter(c=>c&&c.enabled!==false).sort((a,b)=>Number(a.sort_order||0)-Number(b.sort_order||0));const adminCollections=(Array.isArray(state.spikeContent?.spike_collections)?state.spikeContent.spike_collections:[]).filter(c=>c&&c.enabled!==false).sort((a,b)=>Number(a.sort_order||0)-Number(b.sort_order||0));const hasAdminCategories=adminCategories.length>0;const catHtml=hasAdminCategories?adminCategories.slice(0,8).map(c=>`<button class="cat" data-action="open-spike-category" data-id="${esc(c.id)}">${c.image_url?`<img src="${absoluteProductImage(c.image_url)}">`:''}<span>${esc(c.name).replace(/\n/g,'<br>')}</span></button>`).join(''):categories.length?categories.slice(0,8).map(c=>`<button class="cat" data-action="open-category" data-value="${esc(c[1])}"><img src="${A}${c[0]}"><span>${esc(c[1]).replace(/\n/g,'<br>')}</span></button>`).join(''):`<div class="empty-ux">لا توجد أقسام منشورة بعد</div>`;const collectionsHtml=adminCollections.map(c=>`<button class="spike-collection-card" data-action="open-spike-collection" data-destination-type="${esc(c.destination_type||'')}" data-destination-id="${esc(c.destination_id||'')}" aria-label="${esc(c.name||'مجموعة')}" title="${esc(c.name||'مجموعة')}" style="flex:0 0 153px;width:153px;height:101px;padding:0;border:0;border-radius:12px;overflow:hidden;background:#f3f3f3;cursor:pointer"><img src="${absoluteProductImage(c.image_url)}" alt="${esc(c.name||'مجموعة')}" style="display:block;width:153px;height:101px;object-fit:cover"></button>`).join('');const productsHtml=products.length?products.map(card).join(''):`<div class="empty-ux">لا توجد منتجات منشورة بعد</div>`;const storesHtml=storesData.length?storesData.map(s=>`<button class="brand" data-action="open-store" data-store="${s.id}">${esc(s.name)}</button>`).join(''):`<div class="empty-ux">لا توجد متاجر منشورة بعد</div>`;return `<section class="screen">${brandHead(true)}${backendStatusChip()}${(()=>{const bs=(state.spikeContent?.banner_items||[]).filter(b=>b.enabled!==false);const arr=bs.length?bs:[{image_url:A+'Rectangle 636.png',action_type:'none',target:''}];return `<section class="banner-wrap"><div class="banner-scroll drag-scroll">${arr.map(b=>`<button class="banner-card" ${b.action_type!=='none'?`data-action="banner-target" data-type="${esc(b.action_type)}" data-target="${esc(b.target||'')}"`:''}><img class="banner" src="${absoluteProductImage(b.image_url)}"></button>`).join('')}</div><div class="dots"><i></i></div></section>`})()}<section class="section"><div class="title-row"><h2>تسوق حسب الفئة</h2>${(hasAdminCategories||categories.length)?'<button data-nav="categories">عرض الكل</button>':''}</div><div class="categories">${catHtml}</div></section><section class="section"><div class="title-row"><h2>المنتجات</h2>${products.length?'<button data-action="open-category" data-value="الكل">عرض الكل</button>':''}</div><div class="product-strip drag-scroll">${productsHtml}</div></section>${adminCollections.length?`<section class="section spike-home-collections"><div class="title-row"><h2>المجموعات</h2></div><div class="drag-scroll spike-collections-strip" style="display:flex;gap:12px;overflow-x:auto;overflow-y:hidden;scroll-snap-type:x proximity;padding:2px 0 8px;-webkit-overflow-scrolling:touch">${collectionsHtml}</div></section>`:''}<section class="section"><div class="title-row"><h2>المتاجر</h2>${storesData.length?'<button data-nav="stores">عرض الكل</button>':''}</div><div class="brands drag-scroll">${storesHtml}</div></section>${filterUI()}${currencySheet()}</section>${bottom('home')}`}
