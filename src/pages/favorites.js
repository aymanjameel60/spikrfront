function favorites(){const favs=products.filter(p=>state.favs.has(p.id));return `<section class="screen">${simpleHead('المفضلة',favs.length?{titleAction:'clearfavs',titleActionLabel:'إفراغ المفضلة'}:{})}<div class="page-wrap">${favs.length?`<div class="product-grid">${favs.map(card).join('')}</div>`:`<div class="empty-state rich-empty">${icon('heart',38)}<strong>المفضلة فارغة</strong><span>احفظ المنتجات التي أعجبتك لتجدها هنا بسرعة.</span><button class="red-action" data-nav="home">استكشف المنتجات</button></div>`}</div></section>${bottom('profile')}`}