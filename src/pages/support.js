function support(){setTimeout(loadSpikeSupport,0);return `<section class="screen">${simpleHead('خدمة العملاء')}<div class="page-wrap"><div id="spike-support"><div class="spike-skeletons"><div class="spike-skeleton"></div><div class="spike-skeleton"></div></div></div></div></section>${bottom('profile')}`}
async function loadSpikeSupport(){const root=document.getElementById('spike-support');if(!root)return;const X=window.SpikeExperience;if(!X){setTimeout(loadSpikeSupport,100);return}try{const d=await X.getSupportThreads(),threads=d.threads||[];root.innerHTML=`<form id="support-new" class="support-compose"><input name="subject" required maxlength="120" placeholder="موضوع المحادثة"><button>محادثة جديدة</button></form><div class="support-thread-list">${threads.length?threads.map(t=>`<button class="menu-row" data-support-thread="${t.id}"><span class="menu-lucide">${icon('message-circle',21)}</span><span class="menu-label"><b>${esc(t.subject)}</b><small>${new Date(t.updated_at||t.created_at).toLocaleDateString('ar')}</small></span></button>`).join(''):X.emptyState('لا توجد محادثات','ابدأ محادثة جديدة مع خدمة العملاء')}</div><div id="support-chat"></div>`;root.querySelector('#support-new').onsubmit=async e=>{e.preventDefault();const btn=e.currentTarget.querySelector('button'),subject=new FormData(e.currentTarget).get('subject').trim();if(!subject)return;X.loadingButton(btn,true);try{const r=await X.createSupportThread(subject);X.toast('تم إنشاء المحادثة','success');await loadSpikeSupport();if(r.thread?.id)openSpikeThread(r.thread.id)}catch(err){X.toast(err.message,'error')}finally{X.loadingButton(btn,false)}};root.querySelectorAll('[data-support-thread]').forEach(b=>b.onclick=()=>openSpikeThread(b.dataset.supportThread))}catch(e){root.innerHTML=X.errorState(e.message,'loadSpikeSupport()')}}
async function openSpikeThread(id){const X=window.SpikeExperience,box=document.getElementById('support-chat');if(!box)return;box.innerHTML=X.skeleton(2);try{const d=await X.getSupportMessages(id),messages=d.messages||[];box.innerHTML=`<div class="support-chat-box"><h3>المحادثة</h3><div class="support-messages">${messages.length?messages.map(m=>`<div class="support-msg ${m.sender_role==='customer'?'mine':'admin'}">${esc(m.body)}</div>`).join(''):X.emptyState('لا توجد رسائل','اكتب أول رسالة')}</div><form id="support-reply"><input name="body" required placeholder="اكتب رسالتك..."><button>${icon('send',18)}</button></form></div>`;box.querySelector('#support-reply').onsubmit=async e=>{e.preventDefault();const f=e.currentTarget,btn=f.querySelector('button'),body=new FormData(f).get('body').trim();if(!body)return;X.loadingButton(btn,true,'');try{await X.sendSupportMessage(id,body);f.reset();await openSpikeThread(id)}catch(err){X.toast(err.message,'error')}finally{X.loadingButton(btn,false)}}}catch(e){box.innerHTML=X.errorState(e.message,`openSpikeThread('${id}')`)}}
