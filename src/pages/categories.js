function categoriesPage(){
  const live=(Array.isArray(state.liveCategories)?state.liveCategories:[]).filter(c=>c&&c.enabled!==false);
  const body=live.length?live.map(c=>`<button class="cat" data-action="open-spike-category" data-id="${esc(c.id)}">${c.image_url?`<img src="${esc(absoluteProductImage(c.image_url))}" onerror="this.onerror=null;this.src='${A}placeholder-product.svg'">`:''}<span>${esc(c.name||'قسم').replace(/\n/g,'<br>')}</span></button>`).join(''):categories.length?categories.map(c=>`<button class="cat" data-action="open-category" data-value="${esc(c[1])}"><img src="${absoluteProductImage(c[0])}" onerror="this.onerror=null;this.src='${A}placeholder-product.svg'"><span>${esc(c[1]).replace(/\n/g,'<br>')}</span></button>`).join(''):`<div class="empty-state rich-empty">${icon('layout-grid',34)}<strong>لا توجد أقسام</strong><span>أضف الأقسام من لوحة Spike وستظهر هنا تلقائياً.</span></div>`;
  return `<section class="screen">${simpleHead('تسوق حسب الفئة')}<div class="page-wrap"><div class="categories" style="margin-top:5px">${body}</div></div></section>${bottom('home')}`
}
