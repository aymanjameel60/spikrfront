function categoriesPage(){const body=categories.length?categories.map(c=>`<button class="cat" data-action="open-category" data-value="${esc(c[1])}"><img src="${A}${c[0]}"><span>${esc(c[1]).replace(/\n/g,'<br>')}</span></button>`).join(''):`<div class="empty-state rich-empty">${icon('layout-grid',34)}<strong>لا توجد أقسام</strong><span>أضف الأقسام من لوحة Spike وستظهر هنا تلقائياً.</span></div>`;return `<section class="screen">${simpleHead('تسوق حسب الفئة')}<div class="page-wrap"><div class="categories" style="margin-top:5px">${body}</div></div></section>${bottom('home')}`}
