function auth(type='login'){const signup=type==='signup';return `<section class="screen">${simpleHead(signup?'تسجيل الحساب':'تسجيل الدخول')}<div class="page-wrap forms">${signup?`<div class="input-wrap"><input id="auth-name" class="field" placeholder="ادخل اسمك"><span class="input-icon lucide-input-icon">${icon('user',20)}</span></div>`:''}<div class="input-wrap"><input id="auth-email" type="email" class="field" placeholder="البريد الإلكتروني"><span class="input-icon lucide-input-icon">${icon('mail',20)}</span></div><div class="input-wrap"><input id="auth-password" type="password" class="field" placeholder="ادخل كلمة السر"><span class="input-icon lucide-input-icon">${icon('lock-keyhole',20)}</span></div>${signup?`<div class="hint">* يجب أن لا تقل كلمة المرور عن 8 أحرف</div>`:''}<button class="red-action full" data-action="auth" data-mode="${signup?'signup':'login'}">${signup?'تسجيل الحساب':'تسجيل الدخول'}</button><button class="subtle-link" data-nav="${signup?'login':'signup'}">${signup?'تمتلك حساب؟ قم بتسجيل الدخول من هنا':'لا تملك حساباً؟ قم بإنشاء حساب من هنا'}</button></div></section>${bottom('profile')}`}
