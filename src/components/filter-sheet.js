function filterUI(){const cats=categories.map(c=>c[1]);return `<div class="shade ${state.filter?'show':''}" data-action="closefilter"></div><div class="filter-sheet ${state.filter?'show':''}"><button class="close-filter icon-button" data-action="closefilter">${icon('x',20)}</button><h3>فلترة حسب</h3><hr>${cats.length?cats.map(x=>`<label><input type="radio" name="filter" data-action="applyfilter" value="${esc(x)}"> ${esc(x)}</label>`).join(''):'<div class="empty-ux">لا توجد أقسام للفلترة</div>'}</div>`}
