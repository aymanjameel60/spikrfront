function brandHead(withLocation=false){const unread=Number(state.notificationUnreadCount??state.notifications?.filter(n=>!n.read).length??0);return `<div class="head">${status()}<div class="brand-row"><img class="logo" src="${A}logo 2.png">${withLocation?`<button class="location location-button" data-action="open-addresses">${icon('chevron-down',18)} &nbsp; ${state.activeAddress?`${esc(state.activeAddress.label)} - ${esc(state.activeAddress.area)}`:'اختر عنوان التوصيل'}</button>`:`<div class="location"></div>`}<button class="notify icon-button" data-nav="notifications" aria-label="الإشعارات">${icon('bell',22)}${unread>0?`<b class="navbadge notification-badge">${unread>99?'99+':unread}</b>`:''}</button></div>${withLocation?`<div class="search-row"><div class="searchbox">${icon('search',20,'search-lucide')}<input data-action="search" placeholder="ابحث عن المنتجات ..."></div><button class="currency" data-action="open-currency">${currencyShort()}</button></div>`:''}</div>`}